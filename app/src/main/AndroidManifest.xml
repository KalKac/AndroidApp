<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.edu.pwr.s249270.fitnessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessApp">
        <activity android:name=".MainOptionWindow" />
        <activity
            android:name=".MainActivityWindow"
            android:label="@string/title_activity_main_window"
            android:theme="@style/Theme.FitnessApp.NoActionBar" />

        <meta-data
            android:name="android.support.MainMenuActivity"
            android:value="com.example.MainMenuWindow.MainActivityWindow" />

        <activity android:name=".MainMenuWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>